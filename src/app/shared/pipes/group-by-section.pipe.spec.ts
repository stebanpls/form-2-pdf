import { GroupBySectionPipe } from './group-by-section.pipe';

describe('GroupBySectionPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupBySectionPipe();
    expect(pipe).toBeTruthy();
  });
});
